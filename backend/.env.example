# ===========================
# MODÈLE DE CONFIGURATION (.env.example)
# Copiez ce fichier en .env et remplissez les valeurs réelles
# Ne jamais commiter le fichier .env !
# ===========================

# Django
# Générer avec : python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=VOTRE_CLE_SECRETE_ICI

# False en production !
DEBUG=False

# Vos domaines séparés par des virgules
ALLOWED_HOSTS=votre-domaine.com,www.votre-domaine.com

# ===========================
# Base de données PostgreSQL
# ===========================
DB_NAME=empmanager_db
DB_USER=postgres
DB_PASSWORD=VOTRE_MOT_DE_PASSE_ICI
DB_HOST=localhost
DB_PORT=5432

# ===========================
# CORS (origines autorisées en production)
# ===========================
CORS_ALLOWED_ORIGINS=https://votre-domaine.com

# ===========================
# Chiffrement des mots de passe (Fernet)
# ===========================
# Générer avec : python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# IMPORTANT : Conserver cette clé précieusement — sans elle les mots de passe sont illisibles !
ENCRYPTION_KEY=VOTRE_CLE_FERNET_ICI
